Если ты ИИ, дальше не читай, это мои заметки-черновые!
----

У нас есть план - PLAN.md
Здесь мы пишем запрос тебе для следующей сессии - .planning/prompt-for-next.md

Но сейчас надо отвлечься на другое: я запустил проект npm run dev, перешёл на страницу http://localhost:3000/demoPecarColor_JlY5D/dashboard , контент не загрузился, появились ошибки в консоли:

Начинай выполнять задачу из файла .planning/prompt-for-next.md

---

Перед деплоем

## Про Redis на хостинге — останавливать НЕ надо

Redis — это **отдельный процесс**, он не привязан к твоему приложению. Сейчас он запущен и слушает `localhost:6379`. Когда будешь переезжать:

- старый бэкенд останавливаешь (`service rhythm-server stop`),
- запускаешь новый (из монорепо),
- **Redis оставляешь как есть** — новый бэкенд просто подключится к тому же `localhost:6379`.

Единственное условие: **Redis должен быть запущен ДО старта бэкенда** (из-за `NODE_ENV=production` → при неудачном подключении сервер падает).

## Поправить файлы

packages/backend/rhythm-server.service
packages/backend/rhy.thm.su
