import { StateSchema } from 'app/providers/store';
import { StateSchemaTransactions } from '../slice/state-schema';



// export const selectModule  = (state: StateSchema) => state.transactions || {} as StateSchemaTransactions;
// export const selectLoading = (state: StateSchema) => selectModule(state).loading;
// export const selectErrors  = (state: StateSchema) => selectModule(state).errors;
